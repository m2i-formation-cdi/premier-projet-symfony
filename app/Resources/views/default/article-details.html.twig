{% extends 'base_layout.html.twig' %}

{% block content %}
    <h1>{{ article.title }}</h1>
    <h3>{{ article.category }}</h3>
    <p>{{ article.validated?"validé":"non validé" }}</p>
{% endblock %}


